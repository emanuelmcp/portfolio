---
import { Image } from "astro:assets";
import emanuel from "../assets/about/emanuel.png";
import hired from "../assets/about/hired.png";
---

<section
  id="about"
  class="py-24 px-4 border-t border-primary/30 bg-background/50 backdrop-blur-sm"
>
  <div class="container mx-auto max-w-4xl">
    <div class="space-y-10">
      <!-- Título -->
      <h2
        class="text-3xl md:text-4xl font-bold text-accent tracking-tight flex items-center gap-2"
      >
        <span class="text-primary">{">"}</span> whoami
      </h2>
      <!-- Contenedor principal -->
      <div
        class="border border-primary/30 bg-card/80 p-6 md:p-10 shadow-xl hover:shadow-primary/10 transition-shadow duration-300"
      >
        <div
          class="flex flex-col md:flex-row items-center md:items-start gap-10"
        >
          <!-- Imagen con efecto de hover -->
          <div class="relative group w-full md:w-1/3 flex-shrink-0">
            <!-- Glow exterior -->
            <div
              class="absolute -inset-1 bg-gradient-to-tr from-primary/40 to-accent/40 blur opacity-60 group-hover:opacity-30 transition"
            >
            </div>

            <!-- Imagen principal -->
            <Image
              src={emanuel}
              alt="Foto de Emanuel"
              class="relative shadow-2xl object-cover h-full mx-auto transform transition duration-500 group-hover:rotate-2 group-hover:scale-105"
              widths={[271, 320, 480, 640, 768, 1024]}
              sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 271px"
              loading="lazy"
            />
            <!-- Sello "HIRED" -->
            <Image
              src={hired}
              alt="Sello de contratado"
              class="absolute top-1/2 left-1/2 w-[130%] max-w-none opacity-0 transform -translate-x-1/2 -translate-y-1/2 rotate-[-25deg] transition duration-700 ease-out group-hover:opacity-100 group-hover:scale-105 group-hover:rotate-[-20deg]"
              widths={[353, 480, 640, 768, 1024]}
              sizes="(max-width: 768px) 80vw, (max-width: 1024px) 50vw, 353px"
            />
          </div>

          <!-- Texto -->
          <div class="flex-1 space-y-5 text-sm md:text-base leading-relaxed">
            <p class="text-foreground">
              Desarrollador <span class="text-primary font-semibold"
                >fullstack</span
              > especializado en tecnologías modernas, con experiencia en la creación
              de aplicaciones web robustas que superan expectativas. Comprometido
              con la excelencia en desarrollo web para impulsar el crecimiento empresarial
              a través de experiencias de usuario inolvidables.
            </p>
            <p
              class="text-muted-foreground border-l-2 border-primary/40 pl-4 italic"
            >
              Enfocado en escribir código limpio, mantenible y eficiente. Fiel
              creyente en las pruebas automatizadas, la documentación clara y
              las mejores prácticas de DevOps.
            </p>
            <!-- Especialidades -->
            <div class="pt-4 space-y-3">
              <p class="text-primary font-semibold tracking-wide">
                {">"} Especialidades:
              </p>
              <ul class="list-none space-y-1 text-muted-foreground pl-4">
                <li>{"→"} Diseño de APIs RESTful y GraphQL</li>
                <li>{"→"} Arquitecturas de microservicios</li>
                <li>{"→"} Optimización de bases de datos SQL/NoSQL</li>
                <li>{"→"} Sistemas distribuidos y caching</li>
                <li>{"→"} CI/CD y containerización</li>
              </ul>
            </div>
          </div>
        </div>
      </div>

      <!-- Tarjetas inferiores -->
      <div class="grid md:grid-cols-3 gap-6 mt-10">
        {
          [
            { label: "> Experiencia", value: "5+ años" },
            { label: "> Proyectos", value: "50+" },
            { label: "> Café/día", value: "∞" },
          ].map(({ label, value }) => (
            <div class="border border-primary/30 bg-card/80 p-6 text-center shadow-md hover:shadow-primary/10 transition-shadow duration-300">
              <div class="text-accent text-sm mb-2 font-mono">{label}</div>
              <div class="text-3xl font-bold text-primary">{value}</div>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
